First:
I separate X and y.

Then:
I split the data into training and testing sets.

Then:
I create a Pipeline.

The Pipeline preprocesses the data
using StandardScaler.

Then:
The ML model learns using fit().

Finally:
I use predict() to make predictions
on unseen test data.